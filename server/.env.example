PORT=5000
MONGODB_URI=your_atlas_connection_string_including_database_name
JWT_SECRET=your_super_secret_jwt_key
# Optional (only if you want to bootstrap an admin account via /auth/register)
ADMIN_BOOTSTRAP_SECRET=choose_a_secret
# Optional: allow only your frontend(s)
# CORS_ORIGIN=http://localhost:5173

